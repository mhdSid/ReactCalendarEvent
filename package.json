{
  "name": "event_creator",
  "version": "0.1.0",
  "private": true,
  "homepage" : "http://mhdSid.github.io/ReactCalendarEvent",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14",
     "moment": "2.18.1",
     "moment-events": "file:./src/moment-events.js"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "npm run build && gh-pages -d build"
  }
}